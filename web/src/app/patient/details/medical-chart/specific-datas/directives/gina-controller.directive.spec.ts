import { GinaControllerDirective } from './gina-controller.directive';

describe('GinaControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new GinaControllerDirective();
    expect(directive).toBeTruthy();
  });
});
